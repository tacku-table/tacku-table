import { NextPage } from "next";
import MainPage from "./mainPage";

const index: NextPage = () => {
    return (
        <div>
            <MainPage />
        </div>
    );
};

export default index;
