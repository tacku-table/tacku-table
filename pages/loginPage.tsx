import Register from "@/components/auth/Register";
import React from "react";

const loginPage = () => {
    return (
        <div>
            loginPage
            <Register />
        </div>
    );
};

export default loginPage;
